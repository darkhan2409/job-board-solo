# 🔒 Руководство по Безопасности

## ⚠️ КРИТИЧЕСКАЯ БЕЗОПАСНОСТЬ

### Раскрытый API Ключ - Что Делать НЕМЕДЛЕННО

Если вы обнаружили, что ваш OpenAI API ключ был раскрыт в коде, git истории или публичном репозитории:

#### Шаг 1: Отозвать Раскрытый Ключ (СРОЧНО!)

1. Перейдите на https://platform.openai.com/api-keys
2. Войдите в свой аккаунт
3. Найдите раскрытый API ключ в списке
4. Нажмите на иконку удаления (корзина)
5. Подтвердите удаление

**⏰ Это нужно сделать НЕМЕДЛЕННО!** Раскрытый ключ может привести к:
- Несанкционированному использованию вашего API квоты
- Финансовым потерям (списание средств с вашего аккаунта)
- Нарушению лимитов использования OpenAI

#### Шаг 2: Создать Новый Ключ

1. На той же странице https://platform.openai.com/api-keys
2. Нажмите "Create new secret key"
3. Дайте ключу понятное имя (например, "Job Board Dev - 2025-01-XX")
4. **СКОПИРУЙТЕ КЛЮЧ СРАЗУ** - он будет показан только один раз!
5. Сохраните ключ в безопасном месте (менеджер паролей)

#### Шаг 3: Обновить Локальные Файлы

```bash
# 1. Перейдите в директорию frontend
cd frontend

# 2. Откройте .env.local в текстовом редакторе
# 3. Замените старый ключ на новый
OPENAI_API_KEY=sk-proj-ваш-новый-ключ-здесь

# 4. Сохраните файл
```

#### Шаг 4: Очистить Git Историю (если ключ был закоммичен)

**ВНИМАНИЕ:** Эта операция перезапишет git историю!

##### Вариант A: Использовать BFG Repo-Cleaner (Рекомендуется)

```bash
# 1. Скачайте BFG Repo-Cleaner
# https://rtyley.github.io/bfg-repo-cleaner/

# 2. Создайте файл со старым ключом
echo "sk-proj-старый-раскрытый-ключ" > secrets.txt

# 3. Запустите BFG
java -jar bfg.jar --replace-text secrets.txt

# 4. Очистите историю
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# 5. Force push (если репозиторий публичный)
git push --force --all
```

##### Вариант B: Использовать git filter-branch

```bash
# Удалить .env.local из всей истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch frontend/.env.local backend/.env" \
  --prune-empty --tag-name-filter cat -- --all

# Очистить ссылки
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# Force push (ОСТОРОЖНО!)
git push --force --all
```

---

## 🛡️ Лучшие Практики Безопасности

### 1. Управление Секретами

#### ✅ ДЕЛАТЬ:

- Использовать `.env` и `.env.local` файлы для секретов
- Добавить `.env*` в `.gitignore`
- Использовать `.env.example` шаблоны с placeholder значениями
- Хранить production секреты в переменных окружения сервера
- Использовать менеджеры паролей для хранения ключей
- Регулярно ротировать API ключи

#### ❌ НЕ ДЕЛАТЬ:

- Никогда не коммитить `.env` или `.env.local` файлы
- Не хардкодить секреты в коде
- Не делиться ключами в чатах/email
- Не использовать одни и те же ключи для dev/prod
- Не хранить ключи в issue/pull requests/comments

### 2. Структура .env Файлов

#### Frontend (.env.local)
```bash
# ✅ Правильно
OPENAI_API_KEY=sk-proj-...
NEXT_PUBLIC_API_URL=http://localhost:8000

# ❌ Неправильно
OPENAI_API_KEY='sk-proj-...'  # Не используйте кавычки
API_KEY=sk-proj-...  # Используйте полные названия
```

#### Backend (.env)
```bash
# ✅ Правильно
SECRET_KEY=длинная-случайная-строка-минимум-32-символа
DATABASE_URL=sqlite+aiosqlite:///./jobs.db

# ❌ Неправильно
SECRET_KEY=123456  # Слишком простой
SECRET_KEY=your-secret-key-here  # Placeholder в production
```

### 3. Генерация Безопасных Ключей

#### SECRET_KEY для FastAPI

```python
import secrets

# Генерация 32-байтового ключа (256 бит)
secret_key = secrets.token_urlsafe(32)
print(secret_key)
# Пример вывода: "ZYX9Q8W7E6R5T4Y3U2I1O0P-_abcdefghijklmnop"
```

#### Альтернативные методы

```bash
# Используя OpenSSL
openssl rand -base64 32

# Используя Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Используя /dev/urandom (Linux/Mac)
head -c 32 /dev/urandom | base64
```

### 4. Проверка Безопасности

#### Чеклист перед коммитом

- [ ] Все `.env` файлы в `.gitignore`
- [ ] Нет захардкоженных секретов в коде
- [ ] `.env.example` содержит только placeholders
- [ ] Production ключи отличаются от dev ключей
- [ ] API ключи имеют ограничения по правам (scope)

#### Автоматическая проверка

Добавьте pre-commit hook:

```bash
#!/bin/bash
# .git/hooks/pre-commit

# Проверка на наличие секретов
if git diff --cached | grep -E "sk-[a-zA-Z0-9]{48}"; then
  echo "❌ ОШИБКА: Обнаружен OpenAI API ключ!"
  echo "Удалите ключ перед коммитом."
  exit 1
fi

if git diff --cached --name-only | grep -E "\.env$|\.env\.local$"; then
  echo "❌ ОШИБКА: Попытка закоммитить .env файл!"
  echo "Добавьте .env файлы в .gitignore."
  exit 1
fi

echo "✅ Проверка безопасности пройдена"
exit 0
```

### 5. Мониторинг и Оповещения

#### Мониторинг использования OpenAI

1. Перейдите на https://platform.openai.com/usage
2. Настройте лимиты расходов:
   - Settings → Limits → Set monthly budget
   - Установите email оповещения при достижении 50%, 75%, 90%

#### GitHub Secret Scanning

Если используете GitHub:
1. Settings → Code security and analysis
2. Включите "Secret scanning"
3. Включите "Push protection"

---

## 🔍 Что Проверить После Настройки

### Локальная Разработка

```bash
# 1. Проверьте .gitignore
cat .gitignore | grep -E "\.env"
# Должно показать: .env и .env.local

# 2. Проверьте, что .env не в git
git ls-files | grep -E "\.env$|\.env\.local$"
# Не должно ничего показать

# 3. Проверьте, что .env.example есть
ls -la frontend/.env.example backend/.env.example
# Должны существовать оба файла
```

### Production Deployment

- [ ] Все секреты в переменных окружения (не в файлах)
- [ ] DATABASE_URL указывает на production БД
- [ ] DEBUG=False
- [ ] CORS_ORIGINS содержит только production домены
- [ ] Используется HTTPS (не HTTP)
- [ ] Rate limiting настроен
- [ ] Логирование настроено

---

## 📞 Что Делать При Инциденте

### Подозрительная Активность в OpenAI Account

1. **Немедленно отзовите все ключи**
2. **Проверьте Usage страницу** на необычные запросы
3. **Измените пароль аккаунта**
4. **Включите 2FA** (если еще не включена)
5. **Свяжитесь с OpenAI Support** если видите несанкционированное использование

### Раскрытие в Публичном Репозитории

1. **Отзовите ключ** (см. выше)
2. **Удалите репозиторий** временно (если возможно)
3. **Очистите историю** (см. выше)
4. **Создайте новый ключ**
5. **Запушьте очищенную историю**
6. **Мониторьте usage** в течение недели

---

## 📚 Дополнительные Ресурсы

### Официальная Документация

- [OpenAI API Keys Best Practices](https://platform.openai.com/docs/guides/production-best-practices)
- [GitHub Secret Scanning](https://docs.github.com/en/code-security/secret-scanning)
- [OWASP Secrets Management](https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html)

### Инструменты

- [BFG Repo-Cleaner](https://rtyley.github.io/bfg-repo-cleaner/) - Очистка git истории
- [git-secrets](https://github.com/awslabs/git-secrets) - Предотвращение коммита секретов
- [trufflehog](https://github.com/trufflesecurity/trufflehog) - Поиск секретов в git истории
- [detect-secrets](https://github.com/Yelp/detect-secrets) - Pre-commit hook для секретов

---

## ✅ Итоговый Чеклист

### Перед Первым Коммитом

- [ ] `.env` и `.env.local` в `.gitignore`
- [ ] Созданы `.env.example` файлы
- [ ] Настроен pre-commit hook
- [ ] Проверена git история на секреты
- [ ] OpenAI ключ хранится только в `.env.local`

### Регулярное Обслуживание

- [ ] Ротация API ключей каждые 90 дней
- [ ] Проверка usage в OpenAI dashboard
- [ ] Аудит переменных окружения
- [ ] Обновление `.env.example` при добавлении новых переменных

### Перед Production Deployment

- [ ] Все секреты в переменных окружения сервера
- [ ] DEBUG=False
- [ ] Безопасный SECRET_KEY
- [ ] HTTPS настроен
- [ ] Rate limiting активен
- [ ] Мониторинг настроен

---

**Помните: Безопасность - это не одноразовая задача, а непрерывный процесс!** 🛡️
