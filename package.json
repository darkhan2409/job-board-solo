{
  "name": "job-board-solo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "check:screenshots": "ts-node scripts/check-screenshots.ts",
    "validate:documentation": "ts-node scripts/validate-documentation.ts",
    "validate:git": "ts-node scripts/validate-git-history.ts",
    "validate:evidence": "ts-node scripts/validate-evidence.ts",
    "validate:all": "npm run check:screenshots && npm run validate:documentation && npm run validate:git && npm run validate:evidence"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
